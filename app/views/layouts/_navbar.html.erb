<nav class="navbar navbar-expand-sm navbar-dark bg-dark" aria-label="Third navbar example">
  <div class="container-fluid">
    <a class="navbar-brand" href="<%= root_path  %>">Keella</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarsExample03" aria-controls="navbarsExample03" aria-expanded="true" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="navbar-collapse collapse show" id="navbarsExample03" style="">
      <ul class="navbar-nav me-auto mb-2 mb-sm-0">
        <li class="nav-item">
        </li>
      </ul>
      <ul class="navbar-nav me-auto mb-2 mb-sm-0">
        <li class="nav-item">
          <a class="nav-link" aria-current="page" href="<%= workouts_path  %>">Trouver une séance</a>
        </li>
      </ul>
      <ul class="navbar-nav me-1 mb-2 mb-sm-0">
          <a href="<%= users_path %>">
            <div class="w-100">
              <img alt="32x32" class="mr-2 rounded mb-1 bg-dark" style="width: 32px; height: 32px;" src="../assets/iconeuser.png" data-holder-rendered="true">
            </div>
          </a>
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" id="dropdown03" data-bs-toggle="dropdown" aria-expanded="false">Menu</a>
          <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="dropdown03">
            <% if user_signed_in? %> 
            <li><%= link_to "Mes réservations", user_reservations_path(current_user), class: "dropdown-item" %></li>
            <li><%= link_to "Proposer une séance", new_workout_path, class: "dropdown-item" %></li>
            <li><%= link_to "Mon compte", users_path, class: "dropdown-item" %></li>
            <li><%= button_to "Se deconnecter", destroy_user_session_path, method: :delete, data: {turbo: true, turbo_method: :delete, turbo_confirm: "Vous etes sure de vouloir vous déconnecter ?"}, class: "dropdown-item" %></li>
            <% else %>
            <li><%= link_to "Se connecter", new_user_session_path, class: "dropdown-item" %></li>
            <li><%= link_to "S'inscrire", new_user_registration_path, class: "dropdown-item" %></li>
            <% end %>
          </ul>
        </li>
      </ul>
    </div>
  </div>
</nav>